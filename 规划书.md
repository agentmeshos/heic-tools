# HEIC文件查看器 - Windows GUI应用程序规划书

**项目名称**: HEIC图像查看器 (HEIC Image Viewer)
**创建日期**: 2026年2月14日
**项目路径**: d:\7.project\heic-tools

---

## 一、项目概述

### 1.1 项目目标
开发一个基于Python的Windows桌面应用程序,用于浏览和查看HEIC格式的图像文件,提供友好的用户界面和基本的图像操作功能。

### 1.2 目标用户
需要在Windows系统上查看iPhone/iPad等设备拍摄的HEIC格式照片的用户。

### 1.3 现有资源
项目目录下已有5个HEIC测试文件:
- ES7210-ES8311音频.heic
- GigoDevice 25Q32CS1G 32Mflash.heic
- RT9048-GSPABBK5电源.heic
- 互联接口-radar.heic
- 互联接口MCU.heic

---

## 二、功能需求

### 2.1 核心功能

#### 2.1.1 文件浏览
- 支持选择单个HEIC文件打开
- 支持选择文件夹批量加载HEIC文件
- 显示文件列表(缩略图+文件名)

#### 2.1.2 图像查看
- 显示HEIC图像的完整内容
- 自适应窗口大小显示
- 支持图像缩放(放大/缩小/还原)
- 支持图像拖拽移动

#### 2.1.3 文件导航
- 上一张/下一张切换
- 快捷键支持(左右箭头键)
- 文件列表点击跳转

#### 2.1.4 图像转换
- 转换为常见格式(JPG/PNG)
- 批量转换功能
- 自定义输出路径

### 2.2 辅助功能

#### 2.2.1 图像信息显示
- 文件名、大小、分辨率
- EXIF信息(拍摄时间、设备等)

#### 2.2.2 界面优化
- 现代化UI设计
- 响应式布局
- 错误提示和加载状态

---

## 三、技术架构

### 3.1 开发语言
- **Python 3.8+**

### 3.2 核心依赖库

| 库名称 | 版本要求 | 用途 |
|--------|----------|------|
| `tkinter` | 内置 | GUI框架基础 |
| `customtkinter` | ≥5.2.0 | 现代化UI组件 |
| `Pillow` | ≥10.0.0 | 图像处理和显示 |
| `pillow-heif` | ≥0.13.0 | HEIC格式支持 |

### 3.3 技术选型理由
- **CustomTkinter**: 提供现代化外观,无需复杂配置
- **Pillow + pillow-heif**: 兼容性好,文档完善
- **tkinter**: Python内置,无额外依赖

---

## 四、系统设计

### 4.1 项目结构

```
heic-tools/
├── heic-file/                    # 测试HEIC文件目录
├── heic-viewer/                  # 应用程序主目录
│   ├── main.py                   # 应用程序入口
│   ├── gui/
│   │   ├── __init__.py
│   │   ├── main_window.py       # 主窗口类
│   │   ├── image_canvas.py      # 图像显示组件
│   │   └── file_list_panel.py   # 文件列表面板
│   ├── core/
│   │   ├── __init__.py
│   │   ├── heic_loader.py       # HEIC文件加载器
│   │   ├── image_converter.py   # 格式转换器
│   │   └── exif_reader.py       # EXIF信息读取
│   ├── utils/
│   │   ├── __init__.py
│   │   └── helpers.py           # 工具函数
│   ├── resources/
│   │   └── icons/               # 图标资源(可选)
│   ├── requirements.txt         # 依赖列表
│   └── README.md               # 使用说明
├── 规划书.md                     # 本文档
└── TODO.md                      # 任务清单
```

### 4.2 界面布局设计

```
┌───────────────────────────────────────────────────────────┐
│  标题栏: HEIC图像查看器                                     │
├───────────────────────────────────────────────────────────┤
│  [打开文件] [打开文件夹] [转换为JPG] [转换为PNG] [关于]     │
├──────────┬────────────────────────────────────────────────┤
│          │                                                │
│  文件    │                                                │
│  列表    │            图像显示区域                         │
│  面板    │         (支持缩放、拖拽)                        │
│          │                                                │
│ 缩略图   │                                                │
│   +      │                                                │
│ 文件名   │                                                │
│          │                                                │
├──────────┴────────────────────────────────────────────────┤
│  [◀ 上一张]  [▶ 下一张]  [🔍 缩放: 100%]  [重置]           │
│  文件: xxx.heic | 大小: 2.5MB | 分辨率: 1920x1080        │
└───────────────────────────────────────────────────────────┘
```

### 4.3 核心类设计

#### 4.3.1 MainWindow (主窗口)
```python
class MainWindow(ctk.CTk):
    """主窗口类"""
    - __init__()
    - setup_ui()              # 初始化界面布局
    - create_menu_bar()       # 创建菜单栏
    - create_toolbar()        # 创建工具栏
    - create_file_panel()     # 创建文件列表面板
    - create_image_panel()    # 创建图像显示面板
    - create_status_bar()     # 创建状态栏
    - bind_shortcuts()        # 绑定快捷键
    - load_file()             # 加载单个文件
    - load_folder()           # 加载文件夹
    - show_image()            # 显示当前图像
    - next_image()            # 下一张
    - prev_image()            # 上一张
    - convert_current()       # 转换当前文件
    - batch_convert()         # 批量转换
    - update_status()         # 更新状态栏
```

#### 4.3.2 HeicLoader (HEIC加载器)
```python
class HeicLoader:
    """HEIC文件加载器"""
    - __init__()
    - register_heif_support() # 注册HEIC格式支持
    - load_heic(path)         # 加载HEIC文件
    - get_pil_image(path)     # 获取PIL Image对象
    - get_exif_data(path)     # 获取EXIF信息
    - get_thumbnail(path, size) # 获取缩略图
    - validate_heic(path)     # 验证文件有效性
```

#### 4.3.3 ImageConverter (格式转换器)
```python
class ImageConverter:
    """图像格式转换器"""
    - convert_to_jpg(input_path, output_path, quality=95)
    - convert_to_png(input_path, output_path)
    - batch_convert(file_list, output_dir, format, quality)
    - get_output_path(input_path, output_dir, format)
```

#### 4.3.4 ImageCanvas (图像画布)
```python
class ImageCanvas(ctk.CTkFrame):
    """图像显示画布组件"""
    - __init__()
    - load_image(pil_image)   # 加载图像
    - zoom_in()               # 放大
    - zoom_out()              # 缩小
    - reset_zoom()            # 重置缩放
    - fit_to_window()         # 适应窗口
    - enable_drag()           # 启用拖拽
    - on_mouse_wheel()        # 鼠标滚轮缩放
```

#### 4.3.5 FileListPanel (文件列表面板)
```python
class FileListPanel(ctk.CTkScrollableFrame):
    """文件列表面板"""
    - __init__()
    - add_file(file_path)     # 添加文件
    - remove_file(file_path)  # 移除文件
    - clear_files()           # 清空列表
    - select_file(index)      # 选择文件
    - create_thumbnail(path)  # 创建缩略图
    - on_file_click()         # 文件点击事件
```

---

## 五、开发计划

### 5.1 开发阶段

#### 第一阶段:环境搭建 (0.5天)
- 创建项目目录结构
- 安装Python依赖包
- 验证HEIC文件加载功能
- 测试基础GUI框架

#### 第二阶段:核心功能开发 (2天)
- 实现HEIC文件加载器
- 实现主窗口框架
- 实现图像显示功能
- 实现文件打开功能
- 实现基础图像缩放

#### 第三阶段:文件管理功能 (1.5天)
- 实现文件列表面板
- 实现缩略图显示
- 实现文件夹批量加载
- 实现上一张/下一张导航
- 实现快捷键支持

#### 第四阶段:图像转换功能 (1天)
- 实现单文件转换(JPG/PNG)
- 实现批量转换功能
- 实现输出路径选择
- 实现转换进度显示

#### 第五阶段:完善与优化 (1天)
- 实现EXIF信息显示
- 添加错误处理机制
- 性能优化(缓存、线程)
- UI美化和细节调整

#### 第六阶段:测试与打包 (1天)
- 功能测试
- 用户体验优化
- 使用PyInstaller打包为.exe
- 编写使用文档

**预计总开发时间**: 7天

### 5.2 里程碑

| 里程碑 | 完成标准 | 预计日期 |
|--------|----------|----------|
| M1: 基础框架 | 能打开并显示单个HEIC文件 | Day 2 |
| M2: 文件管理 | 能浏览多个文件并切换 | Day 4 |
| M3: 格式转换 | 能转换HEIC为JPG/PNG | Day 5 |
| M4: 发布版本 | 打包为可执行文件 | Day 7 |

---

## 六、技术要点

### 6.1 HEIC文件处理
```python
# 注册HEIC支持
from pillow_heif import register_heif_opener
register_heif_opener()

# 加载HEIC文件
from PIL import Image
image = Image.open("photo.heic")

# 转换为其他格式
image.save("output.jpg", "JPEG", quality=95)
image.save("output.png", "PNG")
```

### 6.2 CustomTkinter基础
```python
import customtkinter as ctk

# 设置主题
ctk.set_appearance_mode("dark")
ctk.set_default_color_theme("blue")

# 创建窗口
app = ctk.CTk()
app.geometry("1200x800")
```

### 6.3 性能优化策略
1. **缩略图缓存**: 避免重复生成
2. **延迟加载**: 仅加载可见区域的缩略图
3. **线程加载**: 使用后台线程加载大文件
4. **图像缩放优化**: 使用适当的重采样算法

### 6.4 错误处理
- 文件不存在或权限不足
- 文件格式错误或损坏
- 内存不足处理
- 转换失败处理

---

## 七、依赖安装

### 7.1 requirements.txt
```
customtkinter>=5.2.0
Pillow>=10.0.0
pillow-heif>=0.13.0
```

### 7.2 安装命令
```powershell
# 创建虚拟环境(推荐)
python -m venv venv
.\venv\Scripts\Activate.ps1

# 安装依赖
pip install -r requirements.txt
```

---

## 八、质量保证

### 8.1 测试计划
- **单元测试**: 核心功能模块(HeicLoader, ImageConverter)
- **集成测试**: 完整工作流程测试
- **用户测试**: 使用现有HEIC文件测试

### 8.2 性能指标
- 应用启动时间: < 2秒
- 单个HEIC文件加载: < 1秒
- 缩略图生成: < 500ms
- 格式转换速度: 取决于文件大小

---

## 九、风险与应对

### 9.1 潜在风险

| 风险项 | 影响 | 概率 | 应对措施 |
|--------|------|------|----------|
| pillow-heif兼容性问题 | 高 | 中 | 提供详细安装文档,测试多个Windows版本 |
| 大文件内存占用 | 中 | 高 | 实现图像缓存和限制加载数量 |
| HEIC文件变体不支持 | 低 | 低 | 添加错误提示,记录不支持的格式 |
| 打包后体积过大 | 低 | 中 | 使用UPX压缩,排除不必要的依赖 |

---

## 十、后续扩展规划

### 10.1 V2.0 功能计划
- 图像基本编辑(旋转、裁剪、调整亮度对比度)
- 幻灯片播放模式
- 支持更多格式(WebP, AVIF, RAW)
- 云存储集成(OneDrive, Google Drive)

### 10.2 V3.0 功能计划
- AI图像增强
- 批量重命名工具
- 图像元数据编辑器
- 多语言支持

---

## 十一、项目交付

### 11.1 交付物清单
- [ ] 完整Python源代码
- [ ] requirements.txt依赖文件
- [ ] README.md使用说明
- [ ] 打包的.exe可执行文件
- [ ] 用户手册文档
- [ ] 开发文档

### 11.2 文档要求
- README: 包含安装、使用、常见问题
- 代码注释: 关键函数和类添加docstring
- 用户手册: 图文并茂的操作指南

---

## 十二、总结

本项目将开发一个轻量级、易用的HEIC文件查看器,采用Python + CustomTkinter + Pillow技术栈,为Windows用户提供便捷的HEIC图像查看和转换功能。

**核心价值**:
1. 解决Windows系统原生不支持HEIC格式的痛点
2. 提供现代化的用户界面
3. 支持批量格式转换
4. 轻量级,易于分发使用

**成功标准**:
- 能稳定打开和显示现有的5个HEIC测试文件
- 界面友好,操作直观
- 转换功能准确无误
- 可打包为独立运行的.exe文件

---

**文档版本**: v1.0
**最后更新**: 2026年2月14日
